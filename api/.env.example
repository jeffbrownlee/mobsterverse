PORT=4000
WEB_URL=http://localhost:3000
NODE_ENV=development

# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=mobsterverse
DATABASE_USER=mobsterverse
DATABASE_PASSWORD=mobsterverse_dev_password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# SMTP Configuration (optional - emails logged to console in dev mode if not set)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@mobsterverse.com
